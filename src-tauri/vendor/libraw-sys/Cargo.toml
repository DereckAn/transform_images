[package]
name = "libraw-sys"
version = "0.4.0"
authors = ["dcuddeback <dcuddeback@gmail.com>"]
description = "FFI bindings to libraw (versión parcheada para macOS)"
license = "MIT"
edition = "2021"

[features]
default = []
static = []  # Nueva feature para enlace estático

[dependencies]
libc = "0.2"

[build-dependencies]
# NO usamos pkg-config ni gcc para evitar el enlace automático de stdc++
# El linking lo maneja el build.rs del proyecto principal
